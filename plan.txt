### Pre-processing of sequencing read data
1. Create genome index (STAR; notebook)
2. Identify the files that belong to your group and
   verify that they are all present and accounted for (UNIX; notebook)
3. Pre-trim QC of fastq files (fastQC; notebook)
4. Trim files (fastq-mcf; notebook)
5. Post-trim QC of fastq files (fastQC; notebook)
6. Align trimmed files to index (STAR; notebook)
### Pre-process and evaulate your count data
1. Identify the count files generated in #A.6 and verify they are
   all accounted for (R; notebook)
2. Create a count matrix in R (R; notebook)
3. Calculate mapping statistics for your libraries  (R dplyr; notebook)
4. Visualize the results from #7  (R ggplot2; notebook)
### Downstream analysis using DESeq2
1. Create DESeq2 data object by summing lane counts
   and merging in metadata (R; notebook)
2. Basic inspection of size factors
3. DE analysis (two-way additive model) with respect to condition
4. Compare results for CNAG_???? from pilot data (R; notebook)
5. DE analysis (two-way additive model) with respect to genotype
6. Compare results for CNAG_???? from pilot data (R; notebook)


### Bonus 1 (if time permits; )
1. Interaction analysis (R; notebook)
2. Estimate DE due to condition within WT (R; notebook)
3. Estimate DE due to condition within sre1d (R; notebook)
4. Compare results #1, 2 and 3 for CNAG_???? from pilot data (R; notebook)



### Bonus 2 (if time permits)
1. Visualize normalized counts for CNAG_04804 (SRE1) with respect to genotype/condition
2. Visualize reads over CNAG_04804 (SRE1)  


### Bonus 3 (if time permits)
1. Combined analysis of all group data.




Compare results by comparing estimated effect sizes (including direction)
and by using dotplots
